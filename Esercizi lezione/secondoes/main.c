#include <stdio.h>

int main() {
    int V[]={1,2,3,4,5,6,7};
    int A,p=10;
    int i=1;
    int N=0;
    int max=V[0];
    for ( i = 1; i < N; ++i)
    {
        if(V[i]>max) {max=V[i];}
    }

}

